{
  "name": "@chainpay/demo-merchant",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm --prefix ../sdk run build && tsc",
    "start": "node dist/server.js",
    "dev": "npm run build && npm start",
    "test": "npm --prefix ../sdk run build && tsc && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@chainpay/sdk": "0.1.0",
    "@solana/web3.js": "^1.98.4",
    "bs58": "^4.0.1",
    "express": "^5.1.0",
    "qs": "6.16.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^26.2.0",
    "typescript": "^5.0.0"
  }
}
